// The Tortoise-compatible range triggers are implemented inline in
// RangeTriggers.h.  Donor-only trigger classes are intentionally not
// reintroduced as a second, incompatible implementation.
#include "RangeTriggers.h"
